version: "3.8"
services:
  klosanow-landing-page:
    image: Image_name:tag
    container_name: landing
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - klosanow-landing
networks:
  klosanow-landing:
    driver: bridge